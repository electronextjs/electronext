import {enConfigAppType} from '../types'

export const DefaultConfig:enConfigAppType = {
  titleBar: {
    visible:true,
    favIcon: true,
    menuIcon: true,
    controlsType: 'windows', 
    titlePosition: 'start'
  }
}